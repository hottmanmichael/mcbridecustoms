'use strict';

// var Notification = require('./global/notification');
// var Modal = require('./global/modal');
// var Ajax = require('./global/ajax').Ajax;
//
// var add_to_featured_buttons = document.querySelectorAll('.add-to-featured');
// var add_to_featured_buttons = document.querySelectorAll('.remove-from-featured');
// var remove_image_buttons = document.querySelectorAll('.delete-image');
//
//
// if (add_to_featured_buttons && add_to_featured_buttons.length > 0) {
//     for (var i = 0; i < add_to_featured_buttons.length; i++) {
//         add_to_featured_buttons[i].addEventListener('click', function(e) {
//             e.preventDefault();
//             console.log("e.action: ", e.target.parentElement.action);
//             // Ajax(url, data, callback)
//             new Notification('warning', e.target.parentElement.action, 3000);
//         });
//     }
// }
//
// if (remove_image_buttons && remove_image_buttons.length > 0) {
//     for (var i = 0; i < remove_image_buttons.length; i++) {
//         remove_image_buttons[i].addEventListener('click', function(e) {
//
//         });
//     }
// }
